apiVersion: v1
kind: Secret
metadata:
  name: demo-secret
  namespace: demo-app
type: Opaque
stringData:
  # Demo-only placeholders. Replace locally before any real deployment.
  APP_USER: demo-user
  APP_PASSWORD: replace-me-before-deployment
