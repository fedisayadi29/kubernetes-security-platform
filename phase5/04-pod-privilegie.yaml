apiVersion: v1
kind: Pod
metadata:
  name: pod-privilegie
  namespace: demo-app
  labels: { app: test-privilegie, owner: fedi-sayadi, environment: dev }
spec:
  containers:
    - name: nginx
      image: nginx:1.27
      securityContext: { runAsNonRoot: true, runAsUser: 101, privileged: true, readOnlyRootFilesystem: true }
      resources: { requests: { cpu: "100m", memory: "64Mi" }, limits: { cpu: "250m", memory: "128Mi" } }
      livenessProbe: { httpGet: { path: /, port: 80 } }
      readinessProbe: { httpGet: { path: /, port: 80 } }
