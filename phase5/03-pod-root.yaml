apiVersion: v1
kind: Pod
metadata:
  name: pod-root
  namespace: demo-app
  labels: { app: test-root, owner: fedi-sayadi, environment: dev }
spec:
  containers:
    - name: nginx
      image: nginx:1.27
      securityContext: { runAsNonRoot: false, readOnlyRootFilesystem: true }
      resources: { requests: { cpu: "100m", memory: "64Mi" }, limits: { cpu: "250m", memory: "128Mi" } }
      livenessProbe: { httpGet: { path: /, port: 80 } }
      readinessProbe: { httpGet: { path: /, port: 80 } }
